# Optional, for developing PyWR Reader — NOT needed to run the app.
# The app itself still needs only requirements.txt (Flask).
#
#   ./.venv/bin/pip install -r requirements-dev.txt
#
# ruff lints the Python (config in ruff.toml):
#   ./.venv/bin/ruff check .
#
# playwright enables tests/test_frontend_smoke.py, which drives the real UI in
# a headless browser. Without it those tests skip and the rest still run:
#   ./.venv/bin/playwright install chromium     # ~90 MB, one time
#   ./run_tests.sh
ruff>=0.6
playwright>=1.40
